# Auto-activate uv-managed venv on entry
if [ -d ".venv" ]; then
    source .venv/bin/activate
fi

# Load .env if present
if [ -f ".env" ]; then
    dotenv .env
fi

# Project root on PYTHONPATH
export PYTHONPATH="$PWD:$PYTHONPATH"
# Phase 1–2: ingestion + embedding
psycopg[binary]>=3.1
pgvector>=0.3
python-dotenv
transformers
FlagEmbedding>=1.2
numpy<2.0

# Phase 3: tree build (UMAP + HDBSCAN + LLM summaries)
umap-learn>=0.5.5
hdbscan>=0.8.33
scikit-learn>=1.3          # silhouette score in cluster.py
openai>=1.40              # summarizer HTTP client (talks to vLLM)

# Phase 4: retrieval (CLI + FastAPI)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5